import { ColoryDirective } from './colory.directive';

describe('ColoryDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoryDirective();
    expect(directive).toBeTruthy();
  });
});
